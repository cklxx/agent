---
description: 
globs: 
alwaysApply: true
---
# Python 环境规范
- 总是在 uv 环境中运行 python 命令
- 使用 `uv venv` 创建虚拟环境
- 使用 `uv sync` 安装依赖

# 代码风格规范
- 使用 Black 格式化 Python 代码
- 使用 isort 排序导入语句
- 使用 flake8 进行代码检查
- 使用 mypy 进行类型检查
- 使用 ESLint 和 Prettier 格式化 JavaScript/TypeScript 代码

# 文档规范
- 所有公共函数和类必须有文档字符串
- 使用 Google 风格的文档字符串格式
- 复杂逻辑必须添加注释说明
- README.md 必须保持最新

# 测试规范
- 所有新功能必须包含单元测试
- 测试覆盖率要求达到 80% 以上
- 使用 pytest 运行测试
- 测试文件命名必须以 test_ 开头

# 版本控制规范
- 提交信息必须清晰描述改动
- 遵循 Conventional Commits 规范
- 重要改动必须创建 Pull Request
- 保持分支命名规范

# 依赖管理规范
- 使用 requirements.txt 管理 Python 依赖
- 使用 package.json 管理 Node.js 依赖
- 定期更新依赖版本
- 锁定依赖版本号
- uv 新增依赖的标准流程：
  - 推荐方式：使用 `uv add 包名` 自动添加到 pyproject.toml 并安装
  - 指定版本：使用 `uv add 包名==版本号` 添加特定版本
  - 添加多个包：使用 `uv add 包名1 包名2` 同时添加多个依赖
  - 添加开发依赖：使用 `uv add --dev 包名` 添加开发依赖
  - 手动添加后同步：编辑 pyproject.toml 后运行 `uv sync` 同步安装

# 配置管理规范
- 敏感信息必须通过环境变量管理
- 提供示例配置文件
- 配置文件必须包含注释说明
- 使用 YAML 或 JSON 格式存储配置

# 错误处理规范
- 所有外部调用必须有错误处理
- 使用自定义异常类
- 提供有意义的错误信息
- 记录关键错误日志

# 性能优化规范
- 避免不必要的循环和递归
- 使用适当的数据结构
- 实现缓存机制
- 优化资源使用

# 安全规范
- 不提交敏感信息到版本控制
- 使用环境变量存储密钥
- 实现适当的访问控制
- 定期进行安全审计

# API 设计规范
- 使用 RESTful 设计原则
- 提供清晰的 API 文档
- 实现适当的错误处理
- 使用版本控制

# 日志规范
- 使用结构化日志格式
- 实现适当的日志级别
- 记录关键操作信息
- 定期清理日志文件

# 工作流程规范
- 遵循项目定义的工作流结构
- 保持状态管理的清晰性
- 使用条件边控制流程
- 实现适当的错误恢复机制